version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 18
        - npm audit fix --force
        - npm install
    build:
      commands:
  artifacts:
    baseDirectory: .amplify-hosting
    files:
      - "**/*"
